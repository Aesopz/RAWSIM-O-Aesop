# M2e-LB（晚綁定 exact）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 M2e 的「規劃」與「槽綁定」解耦——MILP 目標零修改，AllocateOrder 延遲到 bot 認領 pod 的瞬間，在途上限 W 取代 Cs 語意，突破 TP=650 的槽位回收天花板。

**Architecture:** 新鏡像 manager `SplitM1GLBManager`（`SplitM1GExactManager` 逐字鏡像＋提交塊改寫）＋純資料結構 `LbLedger`（遞延帳本，泛型可測）＋引擎唯一 3 行閘控掛鉤（`GetPossibleRequestsofMP` 頭部，null-safe）。Spec: `docs/superpowers/specs/2026-07-12-m2e-lb-design.md`。

**Tech Stack:** C# 7.3 / net48 / Gurobi win64 / RAWSimO.Tests 自製 TestRunner。

## Global Constraints

- Build 只能用：`"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release`（PATH 上的 msbuild 是 v4.0 會假失敗；x86 因 Gurobi 只有 win64 會 runtime 失敗）。
- **絕對不動** `M1GManager.cs`、`HADGSManager.cs`、任何既有 xconf。`SplitM1GExactManager.cs` 本計畫也**不動**（消融對照組）。
- C# 7.3：不可用 C# 8+ 語法；out 參數不能被 lambda 捕獲（先 `var copy = outParam;`）；新檔案要手動加 `<Compile Include>` 進 csproj。
- 新 config 欄位 default 必須 bit-identical（`PlannedWipCap=6` 在 LB 新類型內部是語意錨，不影響任何既有類型；既有 xconf 不引用新類型）。
- XmlSerializer 按宣告順序序列化且靜默丟棄亂序欄位：新欄位加在 `SplitM1GLBConfiguration` 類尾端；xconf 裡 LB 欄位放在 `IdleSlotWeight` 之後。
- xconf 拷貝微調：逐 byte 比對非目標差異區段（含 CRLF）。基準檔 `Material\Instances\CoreBenchmark\small\sweep\sw_0_0.xconf`。
- 測試套件：`RAWSimO.Tests\bin\x64\Release\RAWSimO.Tests.exe`，現有 49/49 全綠是回歸底線。
- 模擬 CLI：`RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe <xlayo> <xsett> <xconf> <outputdir> <seed>`；small 基準：`Material\Instances\CoreBenchmark\small\small.xlayo` + `small_o100_mu100.xsett`。
- SDD ledger：`.superpowers/sdd/progress.md`（gitignored）。
- **本計畫只做一期（純晚綁定）**：`LateBindingBackfill` 欄位會解析但邏輯不實作（永遠 inert）；回填是 W 掃描有果後的獨立二期計畫。

---

### Task 1: 引擎假設審計（唯讀）＋ LbLedger ＋ 單元測試

**Files:**
- Create: `RAWSimO.Core/Control/Defaults/OrderBatching/LbLedger.cs`
- Modify: `RAWSimO.Core/RAWSimO.Core.csproj`（`<Compile Include>`，插在 line 126 `PvgsExactAligned.cs` 之後）
- Create: `RAWSimO.Tests/LbLedgerTests.cs`
- Modify: `RAWSimO.Tests/Program.cs`（line 47 `PvgsExactAlignedTests.Register();` 之後加一行）
- Modify: `RAWSimO.Tests/RAWSimO.Tests.csproj`（`<Compile Include="LbLedgerTests.cs" />`，照 PvgsExactAlignedTests.cs 的既有 include 位置旁邊插）

**Interfaces:**
- Produces: `LbLedger<TOrder, TStation, TPod>`，public API：`Add(order, station, IEnumerable<TPod> suppliers, double plannedAt)`、`int PlannedCount(TStation)`、`List<TOrder> OrdersFor(TPod, TStation)`、`TStation StationOf(TOrder)`、`double PlannedAt(TOrder)`、`bool Contains(TOrder)`、`void Remove(TOrder)`、`List<TOrder> DueOrders(double now, double timeout)`、`int Count { get; }`、`static int RemainingCapacity(int w, int inUse, int reserved, int planned)`。Task 3 以 `LbLedger<Order, OutputStation, Pod>` 實例化。

- [ ] **Step 1: 引擎假設審計（唯讀，產出審計筆記）**

逐點確認以下事實並把結論寫進 `.superpowers/sdd/progress.md`（「LB Task 1 audit」段）。若任何一點與預期不符，**停止並回報**，不要繼續實作：

1. `BotManagerPodSelection.cs` 中 `GetPossibleRequestsofMP` 的呼叫點恰為 9 處（lines ~1169, 1194, 1235, 1256, 1313, 1333, 1383, 1709 附近，加定義 187）——確認沒有其他路徑把 `_Ziops[station]` 的分錄轉成 requests。
2. `GetPossibleRequestsofMP`（lines 187-210）：對每個 `_Ziops[station]` 中該 pod 的 symbol，用 `GetExtractRequestsOfStation(station)` 按 `(Order.ID, Item.ID)` 配對，且 **無論配到幾筆都 `ziops.Remove(oo.Key)` 破壞性消費**（lines 205-207）——這是「綁定必須先於 resolution」的依據。
3. `Allocator.cs:73-90`：`Allocate` 先檢查 `CapacityInUse + 1 > Capacity` 就 throw；`OutputStation.cs:131` `FitsForReservation(order)` = `CapacityInUse + CapacityReserved + 1 <= Capacity`——護欄用後者。
4. `OrderManager.cs:164-172` `AllocateOrder`：`_pendingOrders.Remove(order)` → `station.RegisterOrder(order)` → `Instance.Controller.Allocator.Allocate(order, station)`——LB 在綁定時原樣呼叫，`_pendingOrders.Remove` 對已移除訂單是無害 no-op。
5. `OrderManager.cs:275` `Update` 是 `public virtual`——Task 3 的 override 合法。
6. 確認 `ExtractRequest` 在訂單放置時建立（`ResourceManager.cs:440-447`，pod=null 不綁 pod）——requests 是 per (order, sku)，pod 歸屬只存在 `_Ziops` symbol；因此「訂單一次綁定、各 pod 各自消費自己的 ziops 份額」成立。

- [ ] **Step 2: 寫 LbLedger（完整檔案）**

`RAWSimO.Core/Control/Defaults/OrderBatching/LbLedger.cs`：

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

namespace RAWSimO.Core.Control.Defaults.OrderBatching
{
    /// <summary>
    /// Deferred-binding ledger for the late-binding split manager (SplitM1GLB): holds
    /// planned-but-unbound order allocations keyed by order, carrying the full supplier-pod
    /// set so an order binds when the FIRST of its supplier pods reaches request resolution
    /// (GetPossibleRequestsofMP consumes _Ziops destructively, so binding must precede any
    /// supplier's resolution). Pure data structure with no Instance dependency - generic so
    /// unit tests can drive it with plain strings.
    /// See docs/superpowers/specs/2026-07-12-m2e-lb-design.md.
    /// </summary>
    public class LbLedger<TOrder, TStation, TPod>
    {
        private class Entry
        {
            public TOrder Order;
            public TStation Station;
            public HashSet<TPod> Suppliers;
            public double PlannedAt;
        }

        private readonly Dictionary<TOrder, Entry> _entries = new Dictionary<TOrder, Entry>();
        private readonly Dictionary<TStation, int> _plannedPerStation = new Dictionary<TStation, int>();

        /// <summary>Number of ledgered (planned, unbound) orders for the station.</summary>
        public int PlannedCount(TStation station)
        {
            int n;
            return _plannedPerStation.TryGetValue(station, out n) ? n : 0;
        }

        /// <summary>Total ledgered orders (diagnostics).</summary>
        public int Count { get { return _entries.Count; } }

        /// <summary>Ledgers a planned allocation. Throws on duplicate order (planning must not double-claim).</summary>
        public void Add(TOrder order, TStation station, IEnumerable<TPod> suppliers, double plannedAt)
        {
            _entries.Add(order, new Entry() { Order = order, Station = station, Suppliers = new HashSet<TPod>(suppliers), PlannedAt = plannedAt });
            int n;
            _plannedPerStation.TryGetValue(station, out n);
            _plannedPerStation[station] = n + 1;
        }

        /// <summary>All ledgered orders of the station supplied (at least partly) by the pod, oldest plan first.</summary>
        public List<TOrder> OrdersFor(TPod pod, TStation station)
        {
            return _entries.Values
                .Where(e => e.Station.Equals(station) && e.Suppliers.Contains(pod))
                .OrderBy(e => e.PlannedAt)
                .Select(e => e.Order).ToList();
        }

        /// <summary>Station the order was planned for, or default(TStation) when not ledgered.</summary>
        public TStation StationOf(TOrder order)
        {
            Entry e;
            return _entries.TryGetValue(order, out e) ? e.Station : default(TStation);
        }

        /// <summary>Planning timestamp of the order (PositiveInfinity when not ledgered).</summary>
        public double PlannedAt(TOrder order)
        {
            Entry e;
            return _entries.TryGetValue(order, out e) ? e.PlannedAt : double.PositiveInfinity;
        }

        /// <summary>True when the order is ledgered (planned, unbound).</summary>
        public bool Contains(TOrder order) { return _entries.ContainsKey(order); }

        /// <summary>Removes the order (after binding). No-op when absent.</summary>
        public void Remove(TOrder order)
        {
            Entry e;
            if (!_entries.TryGetValue(order, out e))
                return;
            _entries.Remove(order);
            _plannedPerStation[e.Station] = _plannedPerStation[e.Station] - 1;
        }

        /// <summary>Orders ledgered longer than timeout (watchdog candidates), oldest first.</summary>
        public List<TOrder> DueOrders(double now, double timeout)
        {
            return _entries.Values
                .Where(e => now - e.PlannedAt > timeout)
                .OrderBy(e => e.PlannedAt)
                .Select(e => e.Order).ToList();
        }

        /// <summary>
        /// W-capacity helper: remaining plannable in-flight slots for a station
        /// = W - bound-in-use - bound-reserved - planned-unbound, floored at 0.
        /// </summary>
        public static int RemainingCapacity(int plannedWipCap, int capacityInUse, int capacityReserved, int planned)
        {
            int remaining = plannedWipCap - capacityInUse - capacityReserved - planned;
            return remaining > 0 ? remaining : 0;
        }
    }
}
```

- [ ] **Step 3: csproj include**

`RAWSimO.Core/RAWSimO.Core.csproj` line 126 `<Compile Include="Control\Defaults\OrderBatching\PvgsExactAligned.cs" />` 之後插入：

```xml
    <Compile Include="Control\Defaults\OrderBatching\LbLedger.cs" />
```

- [ ] **Step 4: 寫失敗中的測試（完整檔案）**

`RAWSimO.Tests/LbLedgerTests.cs`（沿用 `PvgsExactAlignedTests.cs` 的 TestRunner 模式）：

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using RAWSimO.Core.Control.Defaults.OrderBatching;

namespace RAWSimO.Tests
{
    public static class LbLedgerTests
    {
        private static LbLedger<string, string, string> NewLedger() { return new LbLedger<string, string, string>(); }

        public static void Register()
        {
            TestRunner.Add("LbLedger_AddIncrementsPlannedCount", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s0", new[] { "p1" }, 10.0);
                l.Add("o2", "s0", new[] { "p2" }, 11.0);
                l.Add("o3", "s1", new[] { "p1" }, 12.0);
                TestRunner.AssertTrue(l.PlannedCount("s0") == 2, "s0 planned=2");
                TestRunner.AssertTrue(l.PlannedCount("s1") == 1, "s1 planned=1");
                TestRunner.AssertTrue(l.PlannedCount("s9") == 0, "unknown station=0");
                TestRunner.AssertTrue(l.Count == 3, "total=3");
            });
            TestRunner.Add("LbLedger_OrdersFor_FiltersPodAndStation", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s0", new[] { "p1", "p2" }, 10.0);
                l.Add("o2", "s0", new[] { "p2" }, 11.0);
                l.Add("o3", "s1", new[] { "p2" }, 12.0);
                var hits = l.OrdersFor("p2", "s0");
                TestRunner.AssertTrue(hits.Count == 2 && hits[0] == "o1" && hits[1] == "o2", "p2@s0 -> o1,o2 oldest first");
                TestRunner.AssertTrue(l.OrdersFor("p1", "s1").Count == 0, "p1@s1 -> none");
            });
            TestRunner.Add("LbLedger_MultiSupplier_AnyPodTriggers", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s0", new[] { "pA", "pB" }, 5.0);
                TestRunner.AssertTrue(l.OrdersFor("pA", "s0").Count == 1, "first supplier pA finds o1");
                TestRunner.AssertTrue(l.OrdersFor("pB", "s0").Count == 1, "supplier pB also finds o1");
            });
            TestRunner.Add("LbLedger_RemoveDecrementsAndForgets", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s0", new[] { "p1" }, 10.0);
                l.Remove("o1");
                TestRunner.AssertTrue(l.PlannedCount("s0") == 0, "planned back to 0");
                TestRunner.AssertTrue(!l.Contains("o1"), "o1 gone");
                TestRunner.AssertTrue(double.IsPositiveInfinity(l.PlannedAt("o1")), "PlannedAt=inf after remove");
                l.Remove("o1"); // no-op, must not throw
                TestRunner.AssertTrue(l.Count == 0, "still empty");
            });
            TestRunner.Add("LbLedger_StationOfAndPlannedAt", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s1", new[] { "p1" }, 42.5);
                TestRunner.AssertTrue(l.StationOf("o1") == "s1", "station recorded");
                TestRunner.AssertTrue(Math.Abs(l.PlannedAt("o1") - 42.5) < 1e-12, "plannedAt recorded");
                TestRunner.AssertTrue(l.StationOf("oX") == null, "unknown order -> default(null)");
            });
            TestRunner.Add("LbLedger_DueOrders_TimeoutOldestFirst", () =>
            {
                var l = NewLedger();
                l.Add("oOld", "s0", new[] { "p1" }, 0.0);
                l.Add("oMid", "s0", new[] { "p1" }, 50.0);
                l.Add("oNew", "s0", new[] { "p1" }, 199.0);
                var due = l.DueOrders(200.0, 100.0); // overdue: age>100 -> oOld(200), oMid(150); oNew(1) not
                TestRunner.AssertTrue(due.Count == 2 && due[0] == "oOld" && due[1] == "oMid", "oldest first, oNew excluded");
            });
            TestRunner.Add("LbLedger_DuplicateAddThrows", () =>
            {
                var l = NewLedger();
                l.Add("o1", "s0", new[] { "p1" }, 1.0);
                bool threw = false;
                try { l.Add("o1", "s0", new[] { "p2" }, 2.0); }
                catch (ArgumentException) { threw = true; }
                TestRunner.AssertTrue(threw, "double-claim must throw");
            });
            TestRunner.Add("LbLedger_RemainingCapacity_ClampsAtZero", () =>
            {
                TestRunner.AssertTrue(LbLedger<string, string, string>.RemainingCapacity(6, 2, 1, 0) == 3, "6-2-1-0=3");
                TestRunner.AssertTrue(LbLedger<string, string, string>.RemainingCapacity(12, 4, 2, 3) == 3, "12-4-2-3=3");
                TestRunner.AssertTrue(LbLedger<string, string, string>.RemainingCapacity(6, 6, 0, 4) == 0, "negative clamps to 0");
            });
        }
    }
}
```

`RAWSimO.Tests/Program.cs` line 47 之後加：

```csharp
            LbLedgerTests.Register();
```

`RAWSimO.Tests/RAWSimO.Tests.csproj`：在 `<Compile Include="PvgsExactAlignedTests.cs" />` 那行旁插入 `<Compile Include="LbLedgerTests.cs" />`。

- [ ] **Step 5: Build 並跑測試確認全綠**

```
"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release
RAWSimO.Tests\bin\x64\Release\RAWSimO.Tests.exe
```

Expected: build 0 errors；測試 57/57 全綠（49 既有 + 8 新）。

- [ ] **Step 6: Commit**

```bash
git add RAWSimO.Core/Control/Defaults/OrderBatching/LbLedger.cs RAWSimO.Core/RAWSimO.Core.csproj RAWSimO.Tests/LbLedgerTests.cs RAWSimO.Tests/Program.cs RAWSimO.Tests/RAWSimO.Tests.csproj
git commit -m "feat(lb): LbLedger deferred-binding ledger + unit tests (M2e-LB Task 1)"
```

---

### Task 2: SplitM1GLBConfiguration ＋ enum ＋ factory case

**Files:**
- Modify: `RAWSimO.Core/Configurations/MethodConfigurationsOB.cs`（`SplitM1GExactConfiguration` 類結尾 line ~1059 的 `}` 之後、PVGS 註解區之前插入新類）
- Modify: `RAWSimO.Core/Configurations/MethodConfiguration.cs`（enum `OrderBatchingMethodType`，line 348 `PVGS,` 之後）
- Modify: `RAWSimO.Core/Control/Controller.cs`（line 123 `case OrderBatchingMethodType.PVGS: ...` 之後）

**Interfaces:**
- Consumes: 無（獨立於 Task 1）。
- Produces: `SplitM1GLBConfiguration : SplitM1GExactConfiguration`，欄位 `int PlannedWipCap = 6`、`double BindingWatchdogTimeout = 180`、`bool LateBindingBackfill = false`；enum 成員 `OrderBatchingMethodType.SplitM1GLB`；factory case `new SplitM1GLBManager(instance)`（Task 3 提供該類，**本 task 先建一個最小殼讓 build 過**——見 Step 2）。

- [ ] **Step 1: 新增 config 類**

`MethodConfigurationsOB.cs`，插在 `SplitM1GExactConfiguration` 的關閉大括號（line ~1059）之後：

```csharp
    /// <summary>
    /// Late-binding variant of SplitM1GExact (M2e-LB): identical MILP, but AllocateOrder is
    /// deferred from solve time to the moment a bot claims the pod trip - planned orders live
    /// in a deferred-binding ledger and only consume physical slot capacity when bound. The
    /// planning admission gate uses PlannedWipCap (W) instead of physical free slots, which is
    /// the load-bearing knob: W=6 reproduces the M2e ceiling, W>6 lets the pod pipeline run
    /// ahead of slot recycling. See docs/superpowers/specs/2026-07-12-m2e-lb-design.md.
    /// </summary>
    public class SplitM1GLBConfiguration : SplitM1GExactConfiguration
    {
        /// <summary>
        /// Returns the type of the corresponding method this configuration belongs to.
        /// </summary>
        /// <returns>The type of the method.</returns>
        public override OrderBatchingMethodType GetMethodType() { return OrderBatchingMethodType.SplitM1GLB; }
        /// <summary>
        /// Returns a name identifying the method.
        /// </summary>
        /// <returns>The name of the method.</returns>
        public override string GetMethodName() { if (!string.IsNullOrWhiteSpace(Name)) return Name; return "OBSPLITM1GLB"; }
        /// <summary>
        /// W: per-station cap on in-flight orders (bound-incomplete + planned-unbound). Replaces
        /// the physical-free-slot semantics of Cs in the MILP. 6 = regression anchor (approximates
        /// legacy M2e admission); sweep upward to let planning run ahead of slot recycling.
        /// </summary>
        public int PlannedWipCap = 6;
        /// <summary>
        /// Seconds after which a planned-unbound order is force-bound by the watchdog (guards
        /// against _Ziops entries stranded by rerouted/cancelled pod trips). Binding still
        /// requires a free reservation slot; blocked watchdog bindings retry each update.
        /// </summary>
        public double BindingWatchdogTimeout = 180;
        /// <summary>
        /// Stage-2 flag (parsed but INERT in stage 1): opportunistic backfill of pending orders
        /// from the claimed pod's residual stock at binding time. Implementation lands in a
        /// follow-up plan only if the W sweep verdict is positive. Default false.
        /// </summary>
        public bool LateBindingBackfill = false;
    }

```

- [ ] **Step 2: enum 成員＋factory case＋最小殼 manager**

`MethodConfiguration.cs` line 348 `PVGS,` 之後：

```csharp
        /// <summary>
        /// Late-binding SplitM1GExact (M2e-LB): same MILP, AllocateOrder deferred to pod-claim
        /// time via a deferred-binding ledger; PlannedWipCap replaces physical Cs semantics.
        /// </summary>
        SplitM1GLB,
```

`Controller.cs` line 123 `case OrderBatchingMethodType.PVGS: ...` 之後：

```csharp
                case OrderBatchingMethodType.SplitM1GLB: OrderManager = new SplitM1GLBManager(instance); break;
```

最小殼（讓本 task build 過；Task 3 整檔取代）——Create `RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GLBManager.cs`：

```csharp
using RAWSimO.Core.Configurations;

namespace RAWSimO.Core.Control.Defaults.OrderBatching
{
    /// <summary>
    /// Late-binding split manager shell - replaced wholesale by the full mirror in the next task.
    /// </summary>
    public class SplitM1GLBManager : SplitM1GExactManager
    {
        /// <summary>
        /// Creates a new instance of this controller.
        /// </summary>
        /// <param name="instance">The instance this controller belongs to.</param>
        public SplitM1GLBManager(Instance instance) : base(instance) { }
    }
}
```

（殼繼承 `SplitM1GExactManager` 僅為編譯通過；Task 3 換成 `: M1GManager` 的完整鏡像。）csproj：`<Compile Include="Control\Defaults\OrderBatching\SplitM1GLBManager.cs" />` 插在 Task 1 加的 `LbLedger.cs` include 之後。

- [ ] **Step 3: Build ＋測試回歸**

```
"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release
RAWSimO.Tests\bin\x64\Release\RAWSimO.Tests.exe
```

Expected: 0 errors、57/57 綠。

- [ ] **Step 4: Commit**

```bash
git add RAWSimO.Core/Configurations/MethodConfigurationsOB.cs RAWSimO.Core/Configurations/MethodConfiguration.cs RAWSimO.Core/Control/Controller.cs RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GLBManager.cs RAWSimO.Core/RAWSimO.Core.csproj
git commit -m "feat(lb): SplitM1GLBConfiguration + enum + factory case + shell manager (M2e-LB Task 2)"
```

---

### Task 3: SplitM1GLBManager 完整鏡像

**Files:**
- Rewrite: `RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GLBManager.cs`（Task 2 的殼整檔取代）
- Reference (read-only): `RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GExactManager.cs`

**Interfaces:**
- Consumes: `LbLedger<Order, OutputStation, Pod>`（Task 1）、`SplitM1GLBConfiguration`（Task 2）。
- Produces: `public bool OnPodRequestResolution(Pod pod, OutputStation station)`——Task 4 的引擎掛鉤呼叫它；回傳 `false` = 槽滿無法綁定，掛鉤方應放棄本次 resolution 保護 `_Ziops` 分錄。

**鏡像規則（憲法 1.3）**：以 `SplitM1GExactManager.cs` 全文為底本逐字拷貝（含全部 private 鏡像方法、starve-aware 包裝、CF 兩段求解塊、決策 log 寫入器），只施加下面列舉的 8 處差異。鏡像忠實度（除列舉差異外 bit-identical 語意）是 reviewer 的 Critical 檢查項。

- [ ] **Step 1: 整檔拷貝＋改名**

拷貝 `SplitM1GExactManager.cs` 全文到 `SplitM1GLBManager.cs`，然後：

**差異 1（類名/ctor/註解）**——class 宣告與建構子：

```csharp
    /// <summary>
    /// Late-binding variant of SplitM1GExactManager (M2e-LB): identical MILP (verbatim mirror),
    /// but AllocateOrder is deferred from solve time to pod-claim time through LbLedger, and the
    /// planning admission gate uses PlannedWipCap (W) instead of physical free slots.
    /// Mirror of SplitM1GExactManager per constitution - the original file stays untouched as the
    /// ablation control. See docs/superpowers/specs/2026-07-12-m2e-lb-design.md.
    /// </summary>
    public class SplitM1GLBManager : M1GManager
    {
        /// <summary>
        /// Creates a new instance of this controller.
        /// </summary>
        /// <param name="instance">The instance this controller belongs to.</param>
        public SplitM1GLBManager(Instance instance) : base(instance)
        {
            _splitConfig = instance.ControllerConfig.OrderBatchingConfig as SplitM1GExactConfiguration;
            _lbConfig = instance.ControllerConfig.OrderBatchingConfig as SplitM1GLBConfiguration;
            _logger = new SplitConsolidationLogger(instance);
            instance.OrderCompleted += _logger.LogParentCompleted;
        }
```

（`_splitConfig` 保持型別 `SplitM1GExactConfiguration`——LB config 是其子類，**MILP 本體因此零改動**。）

**差異 2（新欄位）**——緊接 `_logger` 欄位之後加：

```csharp
        /// <summary>
        /// The LB-specific config (same object as _splitConfig, downcast for the three LB fields).
        /// </summary>
        private SplitM1GLBConfiguration _lbConfig;

        /// <summary>
        /// Deferred-binding ledger: planned-but-unbound allocations (spec §2-§4).
        /// </summary>
        private LbLedger<Order, OutputStation, Pod> _ledger = new LbLedger<Order, OutputStation, Pod>();
```

- [ ] **Step 2: 差異 3——W 口徑容量（GenerateCsLb）**

在鏡像的 `InitializeSplitExact` 內，把底本 line 219 的

```csharp
            Cs = GenerateCs();
```

換成

```csharp
            Cs = GenerateCsLb();
```

並在類內（建議放 `GenerateOdSplit` 鏡像之後）新增：

```csharp
        /// <summary>
        /// LB admission capacity: per-station W minus in-flight (bound-in-use + bound-reserved +
        /// planned-unbound). Replaces the physical-free-slot GenerateCs of the mirror source.
        /// Stations at or over W are omitted (mirror source omits stations at 0 the same way).
        /// </summary>
        private Dictionary<OutputStation, int> GenerateCsLb()
        {
            int w = _lbConfig != null ? _lbConfig.PlannedWipCap : 6;
            Dictionary<OutputStation, int> cs = new Dictionary<OutputStation, int>();
            foreach (var station in Instance.OutputStations)
            {
                int remaining = LbLedger<Order, OutputStation, Pod>.RemainingCapacity(
                    w, station.CapacityInUse, station.CapacityReserved, _ledger.PlannedCount(station));
                if (remaining > 0)
                    cs.Add(station, remaining);
            }
            return cs;
        }
```

- [ ] **Step 3: 差異 4——DecideAboutPendingOrders 提交塊改寫**

鏡像的 `DecideAboutPendingOrders`，把底本 lines 669-682 的

```csharp
            if (R.Count() > 0 && pendingOrders.Count > 0)
            {
                SplitExactSolveResult result = SolveSplitExact(SolverType.Gurobi, PiSKU, OiSKU, allPods, Cs, variableNames,
                    pendingOrders, inboundPods, Ra, Rb, R, Pb, Pa, PodToBot, residuals);
                foreach (var symbol in result.NewZiops)
                {
                    for (int i = 0; i < symbol.Value; i++)
                        symbol.Key.pod.JustRegisterItem(symbol.Key.skui);
                }
                foreach (var alloc in result.Allocations)
                {
                    AllocateOrder(alloc.order, alloc.outputstation);
                    Instance.StatCustomControllerInfo.CustomLogOB1++;
                }
```

換成

```csharp
            if (R.Count() > 0 && pendingOrders.Count > 0 && Cs.Count > 0)
            {
                SplitExactSolveResult result = SolveSplitExact(SolverType.Gurobi, PiSKU, OiSKU, allPods, Cs, variableNames,
                    pendingOrders, inboundPods, Ra, Rb, R, Pb, Pa, PodToBot, residuals);
                foreach (var symbol in result.NewZiops)
                {
                    for (int i = 0; i < symbol.Value; i++)
                        symbol.Key.pod.JustRegisterItem(symbol.Key.skui);
                }
                // LB: defer AllocateOrder to claim time (spec §3). One ledger entry per order,
                // carrying the full supplier-pod set from its solved Ziops rows so the order
                // binds when the FIRST supplier reaches request resolution - protects against
                // GetPossibleRequestsofMP's destructive _Ziops consumption for multi-pod orders.
                foreach (var allocGroup in result.Allocations.GroupBy(a => a.order))
                {
                    Order order = allocGroup.Key;
                    OutputStation station = allocGroup.First().outputstation;
                    HashSet<Pod> suppliers = new HashSet<Pod>(
                        result.NewZiops.Keys.Where(z => z.order == order).Select(z => z.pod));
                    _ledger.Add(order, station, suppliers, Instance.Controller.CurrentTime);
                    // AllocateOrder used to do this at solve time - planning claims the order now,
                    // so the next solve's pending set and the fast lane never see it again.
                    _pendingOrders.Remove(order);
                }
```

（`result.SplitParents` 迴圈與 `Instance.Observer.TimeOrderBatchingbyMP(...)` 照底本不動；`CustomLogOB1++` 移到綁定時刻。）

- [ ] **Step 4: 差異 5——OnPodRequestResolution（綁定核心）**

類內新增：

```csharp
        /// <summary>
        /// Called synchronously by the gated hook at the head of GetPossibleRequestsofMP, BEFORE
        /// the destructive _Ziops matching. Binds (AllocateOrder) every ledgered order of this
        /// station supplied by this pod. Returns false when a binding is blocked by slot capacity
        /// - the caller must then skip the resolution entirely so the _Ziops entries survive for
        /// a later attempt (self-healing retry; only reachable with PlannedWipCap > 6).
        /// </summary>
        /// <param name="pod">The pod whose requests are about to be resolved.</param>
        /// <param name="station">The station the resolution targets.</param>
        /// <returns>True to proceed with resolution, false to abort it.</returns>
        public bool OnPodRequestResolution(Pod pod, OutputStation station)
        {
            List<Order> due = _ledger.OrdersFor(pod, station);
            if (due.Count == 0)
                return true;
            foreach (var order in due)
            {
                if (!station.FitsForReservation(order))
                    return false;
                double plannedAt = _ledger.PlannedAt(order);
                AllocateOrder(order, station);
                _ledger.Remove(order);
                Instance.StatCustomControllerInfo.CustomLogOB1++;
                WriteLbBindingLog(order, pod, station, plannedAt, "claim");
            }
            return true;
        }
```

- [ ] **Step 5: 差異 6——Update override（watchdog）**

類內新增：

```csharp
        /// <summary>
        /// Runs the mirror-source cadence (base intake + situation-gated solve), then the binding
        /// watchdog: orders ledgered longer than BindingWatchdogTimeout are force-bound (guards
        /// against _Ziops entries stranded by rerouted or cancelled pod trips). A slot-blocked
        /// watchdog binding simply retries on a later update.
        /// </summary>
        /// <param name="lastTime">The time before the update.</param>
        /// <param name="currentTime">The time to update to.</param>
        public override void Update(double lastTime, double currentTime)
        {
            base.Update(lastTime, currentTime);
            double timeout = _lbConfig != null ? _lbConfig.BindingWatchdogTimeout : 180.0;
            foreach (var order in _ledger.DueOrders(currentTime, timeout))
            {
                OutputStation station = _ledger.StationOf(order);
                if (station == null || !station.FitsForReservation(order))
                    continue;
                double plannedAt = _ledger.PlannedAt(order);
                AllocateOrder(order, station);
                _ledger.Remove(order);
                Instance.StatCustomControllerInfo.CustomLogOB1++;
                WriteLbBindingLog(order, null, station, plannedAt, "watchdog");
            }
        }
```

- [ ] **Step 6: 差異 7——lb_bindings.csv 寫入器**

類內新增（照鏡像底本 `WriteExactDecisionLog` 的寫入器風格）：

```csharp
        // ── Per-binding logger: the plan->bind gap distribution is the thesis-facing evidence
        //    of the late-binding mechanism (spec §5) ──
        private System.IO.StreamWriter _lbBindingLog;
        private void WriteLbBindingLog(Order order, Pod pod, OutputStation station, double plannedAt, string trigger)
        {
            if (_lbBindingLog == null)
            {
                string dir = Instance != null && Instance.SettingConfig != null ? Instance.SettingConfig.StatisticsDirectory : null;
                if (string.IsNullOrEmpty(dir))
                    dir = ".";
                if (!System.IO.Directory.Exists(dir))
                    System.IO.Directory.CreateDirectory(dir);
                _lbBindingLog = new System.IO.StreamWriter(System.IO.Path.Combine(dir, "lb_bindings.csv"), false) { AutoFlush = true };
                _lbBindingLog.WriteLine("order,pod,station,plannedAt,boundAt,gap,trigger");
            }
            double now = Instance.Controller.CurrentTime;
            _lbBindingLog.WriteLine(string.Join(",", new string[] {
                order.ID.ToString(),
                pod != null ? pod.ID.ToString() : "-1",
                station.ID.ToString(),
                plannedAt.ToString(System.Globalization.CultureInfo.InvariantCulture),
                now.ToString(System.Globalization.CultureInfo.InvariantCulture),
                (now - plannedAt).ToString(System.Globalization.CultureInfo.InvariantCulture),
                trigger
            }));
        }
```

**差異 8（決策 log 檔名不變）**：鏡像保留 `splitm1gx_decision_log.csv` 檔名與欄位（每 run 目錄獨立，分析腳本可直接重用）。

- [ ] **Step 7: Build ＋測試回歸**

```
"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release
RAWSimO.Tests\bin\x64\Release\RAWSimO.Tests.exe
```

Expected: 0 errors、57/57 綠。（此時 LB 型別可實例化但引擎掛鉤未接——`OnPodRequestResolution` 永遠不會被呼叫，訂單會被 ledger 卡住直到 watchdog；**先不跑 LB 模擬**，Task 4 接掛鉤後才有意義。）

- [ ] **Step 8: Commit**

```bash
git add RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GLBManager.cs
git commit -m "feat(lb): SplitM1GLBManager full mirror - deferred binding via LbLedger, W-based Cs, watchdog (M2e-LB Task 3)"
```

---

### Task 4: 引擎閘控掛鉤（3 行）＋全套迴歸

**Files:**
- Modify: `RAWSimO.Core/Control/BotManagerPodSelection.cs`（`GetPossibleRequestsofMP` 方法體開頭，line ~189）

**Interfaces:**
- Consumes: `SplitM1GLBManager.OnPodRequestResolution(Pod, OutputStation) : bool`（Task 3）。

- [ ] **Step 1: 加掛鉤**

`GetPossibleRequestsofMP`（line 187-210）方法體第一行（`// Match fitting items with requests` 之前）插入：

```csharp
            // Late-binding hook (SplitM1GLB only): materialize deferred allocations for this
            // pod/station BEFORE the matching below - the loop consumes _Ziops destructively,
            // so an unbound order's supply would be lost. Returns false when slot capacity
            // blocks binding: skip the resolution entirely so the _Ziops entries survive for a
            // later attempt. Null (every other manager) = no-op.
            SplitM1GLBManager lbManager = Instance.Controller.OrderManager as SplitM1GLBManager;
            if (lbManager != null && !lbManager.OnPodRequestResolution(pod, station))
                return new List<ExtractRequest>();
```

（需要時在檔頭 using 區加 `using RAWSimO.Core.Control.Defaults.OrderBatching;`——先檢查是否已有。）

- [ ] **Step 2: Build**

```
"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release
```

Expected: 0 errors。

- [ ] **Step 3: 非 LB 臂 bit-identical 迴歸（Critical）**

重跑兩個既有基準臂 seed 0，與磁碟上已存的驗收輸出比對 KPI：

```
RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe Material\Instances\CoreBenchmark\small\small.xlayo Material\Instances\CoreBenchmark\small\small_o100_mu100.xsett Material\Instances\CoreBenchmark\small\sweep\sw_0_0.xconf output_lbregr_m2ea_s0 0
RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe Material\Instances\CoreBenchmark\small\small.xlayo Material\Instances\CoreBenchmark\small\small_o100_mu100.xsett Material\Instances\CoreBenchmark\small\pvgs_e.xconf output_lbregr_pvgse_s0 0
```

比對（PowerShell）：

```powershell
$a = Get-Content "output_acc_m2ea_s0\1-2-2-10-0.84-small_o100_mu100-split_milp_m2ea-0\statistics.txt" | Select-String "^KPI_|^StatAverageThroughputTime|^StatOverallOrdersHandled"
$b = Get-Content (Get-ChildItem "output_lbregr_m2ea_s0" -Recurse -Filter statistics.txt).FullName | Select-String "^KPI_|^StatAverageThroughputTime|^StatOverallOrdersHandled"
Compare-Object $a $b
```

Expected: 兩臂皆**零差異**（掛鉤對非 LB manager 是 null no-op）。pvgs_e 對照組同法比 `output_acc_pvgse_s0`。任何差異＝Critical，先修再前進。

- [ ] **Step 4: LB 首跑冒煙（W=6，臨時 xconf）**

用 Write 建臨時檔 `scratch_lb_w6.xconf`（拷貝 `sweep\sw_0_0.xconf` 全文，僅三處改動：頂層 `<Name>lb_w6</Name>`；`xsi:type="SplitM1GLBConfiguration"`；`</IdleSlotWeight>` 行後加三行 `<PlannedWipCap>6</PlannedWipCap>`、`<BindingWatchdogTimeout>180</BindingWatchdogTimeout>`、`<LateBindingBackfill>false</LateBindingBackfill>`），然後：

```
RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe Material\Instances\CoreBenchmark\small\small.xlayo Material\Instances\CoreBenchmark\small\small_o100_mu100.xsett scratch_lb_w6.xconf output_lbsmoke_w6_s0 0
```

Expected（全部達成才算過）:
1. 跑完無 exception；
2. 輸出目錄有 `lb_bindings.csv` 且行數 > 400（多數訂單走 ledger）；
3. `trigger` 欄 `watchdog` 占比 < 5%（認領掛鉤是主通道）；
4. `statistics.txt` 的 `StatOverallOrdersHandled` 在 600-680 帶（W=6 ≈ M2e 行為帶；離譜偏低＝綁定管線壞了）；
5. `StatAverageThroughputTime` < 133（駐留應開始下降；此為方向檢查非門檻）。

- [ ] **Step 5: 行為探針（W 解析驗證）**

同法建 `scratch_lb_w1.xconf`（`<PlannedWipCap>1</PlannedWipCap>`，其餘同 w6）跑 seed 0。Expected: `StatOverallOrdersHandled` 明顯低於 W=6 臂（每站在途壓到 1 必然限流）——證明 `PlannedWipCap` 真的被 XmlSerializer 解析（防欄位順序靜默丟棄）。跑完刪兩個 scratch xconf 與冒煙輸出目錄。

- [ ] **Step 6: Commit**

```bash
git add RAWSimO.Core/Control/BotManagerPodSelection.cs
git commit -m "feat(lb): gated late-binding hook at GetPossibleRequestsofMP head + regression verified (M2e-LB Task 4)"
```

---

### Task 5: lb_w* xconf 檔（byte-rule）

**Files:**
- Create: `Material/Instances/CoreBenchmark/small/lb_w6.xconf`、`lb_w9.xconf`、`lb_w12.xconf`、`lb_w18.xconf`

**Interfaces:**
- Consumes: `SplitM1GLBConfiguration` 解析（Task 4 已驗證）。

- [ ] **Step 1: 產生四檔**

以 `Material\Instances\CoreBenchmark\small\sweep\sw_0_0.xconf` 為底本整檔拷貝，每檔只改：
1. 頂層 `<Name>` → `lb_w6` / `lb_w9` / `lb_w12` / `lb_w18`；
2. `<OrderBatchingConfig xsi:type="SplitM1GExactConfiguration">` → `xsi:type="SplitM1GLBConfiguration"`；
3. `<IdleSlotWeight>0</IdleSlotWeight>` 之後插入（縮排對齊同層）：

```xml
    <PlannedWipCap>6</PlannedWipCap>
    <BindingWatchdogTimeout>180</BindingWatchdogTimeout>
    <LateBindingBackfill>false</LateBindingBackfill>
```

（w9/w12/w18 檔的 `PlannedWipCap` 值對應改。）

- [ ] **Step 2: byte-rule 驗證**

```bash
for f in lb_w6 lb_w9 lb_w12 lb_w18; do
  diff <(grep -v -E "<Name>|xsi:type=\"SplitM1G|PlannedWipCap|BindingWatchdogTimeout|LateBindingBackfill" "Material/Instances/CoreBenchmark/small/$f.xconf") \
       <(grep -v -E "<Name>|xsi:type=\"SplitM1G" "Material/Instances/CoreBenchmark/small/sweep/sw_0_0.xconf") && echo "$f OK"
done
```

Expected: 四檔皆 OK（排除允許差異行後與底本逐行一致；換行符一致由 `diff` 無輸出保證）。

- [ ] **Step 3: 每檔冒煙解析**

四檔各跑 seed 0（同 Task 4 Step 4 的 CLI 格式，輸出 `output_lbx_<name>_s0`），確認 controller.txt 顯示對應 Name、無 exception。跑完刪輸出目錄。

- [ ] **Step 4: Commit**

```bash
git add Material/Instances/CoreBenchmark/small/lb_w6.xconf Material/Instances/CoreBenchmark/small/lb_w9.xconf Material/Instances/CoreBenchmark/small/lb_w12.xconf Material/Instances/CoreBenchmark/small/lb_w18.xconf
git commit -m "feat(lb): lb_w6/9/12/18 xconfs (byte-rule vs sw_0_0 base) (M2e-LB Task 5)"
```

---

### Task 6: 驗收階梯（controller 執行，不派 implementer subagent）

**Files:**
- Create: `run_lb_ladder.cmd`（照 `run_pvgse_accept.cmd` 模式）
- Create: `docs/2026-07-12-m2e-lb-acceptance.md`（結果報告）

**Interfaces:**
- Consumes: Task 5 的四個 xconf；既有 `output_acc_*`（M1G/M2e/PVGS-E 5-seed 基準）；`Material\Instances\CoreBenchmark\small\pvgs_m2e.xconf`（最強貪婪臂 θ=6）。

- [ ] **Step 1: W 掃描（seeds 0/1 初篩）**

`run_lb_ladder.cmd`（`Start-Process -WindowStyle Hidden` 脫離 harness 行程樹＋Monitor tail log）：lb_w6/w9/w12/w18 × seeds {0,1} → `output_lbsw_<w>_s<seed>`。

- [ ] **Step 2: 階梯判定（級 0-2）**

- 級 0（迴歸）已在 Task 4 Step 3 完成——引用其結論。
- 級 1：lb_w6 兩 seed 平均——`StatAverageThroughputTime` 顯著 < 132.9（目標 ≤ ~120）且 `StatOverallOrdersHandled` ≥ M2e 同 seed −2%。
- 級 2：最佳 W 臂——TP > 650（突破天花板）且 RD（`traveleddistanceprogression` 總距）/EOR 不劣於 M2e。逐臂記錄 `lb_bindings.csv` 的 gap 分位數與 watchdog 占比。

- [ ] **Step 3: 最佳臂 5-seed ＋級 3 判定**

最佳 W × seeds {0..4} → 對照 `output_acc_*` 與 pvgs_m2e 5-seed（如缺則補跑）。級 3：五項（TP/PO/RD/OD/EOR）≥ 原版 PVGS-M2e。**級 3 未達不算失敗**——級 2 達成即寫報告，回填二期與否交使用者決策。

- [ ] **Step 4: 寫驗收報告＋commit**

`docs/2026-07-12-m2e-lb-acceptance.md`：階梯逐級判定表、W-U 型曲線、駐留分佈前後對比、gap 分佈、與診斷預測（650→740）的對照。Commit 報告與 `run_lb_ladder.cmd`。

---

## Self-Review 紀錄

- **Spec 覆蓋**：§0 錨點 1（目標不動）=Task 3 鏡像規則；錨點 2（認領時綁定）=Task 3 差異 5＋Task 4 掛鉤；錨點 3（回填旗標）=Task 2 欄位（inert，二期計畫另立——spec §0.3 兩階段語意）；錨點 4（W 旋鈕）=Task 3 差異 3＋Task 5 掃描檔＋Task 6 掃描；錨點 5（不可逆）=Task 3 差異 4（ledger 無 undo）；錨點 6（鏡像）=Task 3；錨點 7（驗收）=Task 6。§4 槽位護欄=差異 5 `FitsForReservation`＋掛鉤 false 路徑；watchdog=差異 6；§5 lb_bindings=差異 7；§7 引擎審計=Task 1 Step 1。
- **無 placeholder**：回填邏輯明確宣告為二期獨立計畫（非 TBD——本計畫交付物不含它）。
- **型別一致**：`OnPodRequestResolution(Pod, OutputStation) : bool` 在 Task 3 差異 5 與 Task 4 掛鉤一致；`LbLedger` API 在 Task 1 定義與 Task 3 消費一致（`OrdersFor`/`StationOf`/`PlannedAt`/`Remove`/`DueOrders`/`PlannedCount`/`RemainingCapacity`）。
- **已知簡化（記錄非缺陷）**：(a) 觸發閘門沿用 base `Update` 的實體空槽口徑——規劃爆發自然發生在完成/新單事件點，W>6 的容量在這些爆發點被消耗；不覆寫 base 的取單迴圈（private 欄位 `idoforder` 不可及，也不必要）。(b) `SplitParents` 的 `TimeStampSubmit` 仍在 solve 時記錄（統計語意同底本）。

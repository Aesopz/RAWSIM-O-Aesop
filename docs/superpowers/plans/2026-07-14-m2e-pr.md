# M2e-PR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 SplitM1GExactManager 的 MILP 中以「真完成語意 zfin（B）＋ pro-rata 部分揀取獎勵（R/D_o）」取代 zdonex all-or-nothing 獎勵，破除死區與缺貨切片套利。

**Architecture:** 就地旗標改造（照 CF/w4/w5 前例）：`SplitM1GExactConfiguration` 加 2 欄位，`SolveSplitExact` 以 `prMode` 分閘改 3 處（z 係數、pro-rata 目標項、eM2done→eZfin），決策 log 加 2 個觀測欄。零新變數、零新限制式族、`B=0` 走原 code path。Spec：`docs/superpowers/specs/2026-07-14-m2e-pr-design.md`。

**Tech Stack:** C# 7.3 / net48 / Gurobi (win64) / RAWSimO.CLI 模擬驗證。

## Global Constraints

- Build 指令（唯一合法形式，PATH 上的 msbuild 是 v4.0 會假失敗）：
  `& "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release /v:m /nologo`
  成功判準：輸出含 `0 個錯誤` / `0 Error(s)`。
- **絕對不碰** `M1GManager.cs`、`HADGSManager.cs`。`SplitM1GExactManager.cs` 是自有檔案，可就地改。
- C# 7.3：無 switch expression、無 range operator；out 參數不可被 lambda 捕獲。
- 本計畫**不含單元測試**：改動全在 Gurobi-bound 的 solver 方法內，驗證走 build + Gate 0-3 模擬迴歸（見 Task 4-6）。這是專案既有慣例（CF/w4/w5 同型改動皆如此）。
- xconf 拷貝微調規則：先 `Copy-Item` 再用 Edit 工具做最小字串替換（保 CRLF），驗收時 `diff` 排除允許差異行後必須零差異。
- 長跑模擬一律 `.cmd` + `Start-Process -WindowStyle Hidden`，不用前景指令。
- 進度 ledger：`.superpowers/sdd/progress.md`（每 task 完成後記錄）。
- 行號基準：commit `6a92c1a` 的 `SplitM1GExactManager.cs`；工作樹另有未 commit 的 LB 改動（`BotManagerPodSelection.cs`／`MethodConfiguration.cs`／`SplitM1GLBManager.cs`），**不要動它們、也不要 commit 它們**——commit 時必須逐檔指名 `git add <file>`，禁止 `git add -A`。

---

### Task 1: Config 欄位（TrueCompletionReward / ProRataReward）

**建議 model：haiku（抄寫型）**

**Files:**
- Modify: `RAWSimO.Core/Configurations/MethodConfigurationsOB.cs`（`SplitM1GExactConfiguration`，class 結尾 line ~1058 `PodSelectTiebreakCost` 之後）

**Interfaces:**
- Produces: `SplitM1GExactConfiguration.TrueCompletionReward`（double，預設 0）、`SplitM1GExactConfiguration.ProRataReward`（double，預設 0）——Task 2 以 `_splitConfig.TrueCompletionReward`／`_splitConfig.ProRataReward` 讀取。
- 無新型別 → **不需要** XmlInclude 或 enum/factory 改動。

- [ ] **Step 1: 加入兩個欄位**

在 `MethodConfigurationsOB.cs` 中 `SplitM1GExactConfiguration` 類別內、`public double PodSelectTiebreakCost = 0.01;` 之後（class 的閉括號之前）插入：

```csharp
        /// <summary>
        /// (M2e-PR) B: true-completion bonus per order whose REMAINING demand is fully
        /// assigned this solve, under unfiltered zfin semantics - out-of-stock SKUs force
        /// zfin=0 instead of being silently skipped the way eM2done does (the missing-SKU
        /// slice-arbitrage hole). Replaces OrderRewardWeight's zdonex reward when active.
        /// Only defined for CrossTime=true (eM1 is an all-or-nothing equality - no partial
        /// exists to reward). 0 = feature off, legacy zdonex/w2 path, bit-identical.
        /// Spec: docs/superpowers/specs/2026-07-14-m2e-pr-design.md.
        /// </summary>
        public double TrueCompletionReward = 0;
        /// <summary>
        /// (M2e-PR) R: pro-rata reward. Each assigned unit of order o earns R/D_o where
        /// D_o is the order's ORIGINAL overall demand (GetDemandCount(), includes
        /// currently out-of-stock SKUs) so slice rewards across epochs sum to exactly R.
        /// Only read when TrueCompletionReward &gt; 0. Keep R &lt;= TrueCompletionReward;
        /// the completion hierarchy R*frac &lt; R &lt; B+R holds structurally for any R&gt;0.
        /// </summary>
        public double ProRataReward = 0;
```

- [ ] **Step 2: Build 驗證**

執行 Global Constraints 的 build 指令。預期：`0 個錯誤`。

- [ ] **Step 3: Diff 紀律檢查**

Run: `git diff --stat`
預期：**只有** `RAWSimO.Core/Configurations/MethodConfigurationsOB.cs` 新增於本 task（工作樹既有的 3 個 LB 檔案改動照舊列出，不屬本 task，不得增減）。

- [ ] **Step 4: Commit（指名檔案）**

```powershell
git add RAWSimO.Core/Configurations/MethodConfigurationsOB.cs
git commit -m "feat(pr): TrueCompletionReward + ProRataReward config fields (M2e-PR Task 1)"
```

---

### Task 2: SolveSplitExact prMode 三處改動 + 決策 log 2 欄

**建議 model：sonnet（整合型）**

**Files:**
- Modify: `RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GExactManager.cs`
  - (a) line ~349 之後：prMode 宣告
  - (b) line ~377 / ~381：z 係數
  - (c) line ~397-399 eps 區塊之後：pro-rata 目標項
  - (d) line ~523-538：eM2done → eZfin 分閘
  - (e) line ~170-199 / ~632-641：決策 log 簽名 + 2 欄 + 兩個呼叫點

**Interfaces:**
- Consumes: Task 1 的 `TrueCompletionReward`／`ProRataReward`；`Order.GetDemandCount()`（回傳 int `_overallQuantity`，原始總需求）。
- Produces: `splitm1gx_decision_log.csv` 尾端新增欄 `partialOrders,partialOnlyNewTrips`（Task 5/6 的解析依賴此欄名）。

- [ ] **Step 1: (a) prMode 宣告**

找到（line ~349）：

```csharp
            double w5 = _splitConfig != null ? _splitConfig.ProcessingPodDrawReward : 0;
```

在其**後**插入：

```csharp
            // (PR) M2e-PR mode (spec: docs/superpowers/specs/2026-07-14-m2e-pr-design.md):
            // TrueCompletionReward B > 0 switches the z reward to unfiltered true-completion
            // semantics (eZfin, out-of-stock SKUs force zfin=0) and adds the R/D_o pro-rata
            // layer. Only defined for CrossTime (eM1 is an all-or-nothing equality - no
            // partial exists to reward); silently inert otherwise. B=0 keeps the legacy
            // zdonex/w2 path bit-identical.
            bool prMode = _splitConfig != null && _splitConfig.TrueCompletionReward > 0 && _splitConfig.CrossTime;
            double prB = prMode ? _splitConfig.TrueCompletionReward : 0;
            double prR = prMode ? _splitConfig.ProRataReward : 0;
            double zRewardCoeff = prMode ? -prB : w2;
```

- [ ] **Step 2: (b) z 係數換成 zRewardCoeff（兩處）**

line ~377（`Ra.Count() > 0` 分支）與 line ~381（else 分支），把

```csharp
                    + LinearExpression.Sum(deVarNamez.Select(v => variablesBinary[v.name])) * w2
```

兩處都改為：

```csharp
                    + LinearExpression.Sum(deVarNamez.Select(v => variablesBinary[v.name])) * zRewardCoeff
```

（注意兩處縮排不同：else 分支那行前導空白較少，逐字保持原縮排。`w2` 本即負值、`-prB` 同號向，形式一致。）

- [ ] **Step 3: (c) pro-rata 目標項**

找到 eps 區塊（line ~397-399）：

```csharp
            double eps = _splitConfig != null ? _splitConfig.UnitDrawReward : 0;
            if (eps != 0 && deVarNameq.Count > 0)
                objective = objective + LinearExpression.Sum(deVarNameq.Select(v => variablesQ[v.name])) * eps;
```

在其**後**插入：

```csharp
            // (PR) pro-rata layer: every assigned unit of order o earns R/D_o (negative =
            // reward under Minimize). D_o = the order's ORIGINAL overall demand
            // (GetDemandCount(), includes currently out-of-stock SKUs) so slice rewards
            // across epochs sum to exactly R (spec section 3.4) - a remaining-demand
            // denominator would let every epoch's slice count from 100% and resurrect the
            // slice arbitrage. Guarded like eps: no q variables = term absent
            // (LinearExpression.Sum throws on an empty sequence).
            if (prMode && prR != 0 && deVarNameq.Count > 0)
                objective = objective + LinearExpression.Sum(deVarNameq.Select(v => variablesQ[v.name] * (-prR / v.order.GetDemandCount())));
```

- [ ] **Step 4: (d) eM2done → eZfin 分閘**

把整個完成連結迴圈（line ~523-538）：

```csharp
            // (eM1/eM2/eM2done) per-SKU completion linkage, aggregated across stations AND pods
            foreach (var order in pendingOrders)
            {
                string zname = (crossTime ? "zdonex" : "zfullx") + "_" + order.ID.ToString();
                foreach (var sku in residuals[order].Where(p => PiSKU.ContainsKey(p.Key)))
                {
                    var lhs = LinearExpression.Sum(deVarNameq.Where(v => v.order.ID == order.ID && v.skui.ID == sku.Key.ID).Select(v => variablesQ[v.name]));
                    if (!crossTime)
                        wrapper.AddConstr(lhs == sku.Value * variablesBinary[zname], "eM1");
                    else
                    {
                        wrapper.AddConstr(lhs <= sku.Value, "eM2");
                        wrapper.AddConstr(lhs >= sku.Value * variablesBinary[zname], "eM2done");
                    }
                }
            }
```

整段替換為：

```csharp
            // (eM1/eM2/eM2done | eZfin) per-SKU completion linkage, aggregated across stations AND pods
            foreach (var order in pendingOrders)
            {
                string zname = (crossTime ? "zdonex" : "zfullx") + "_" + order.ID.ToString();
                if (prMode)
                {
                    // (eZfin) true-completion semantics (spec section 3.3): iterate the FULL
                    // remaining demand - no PiSKU visibility filter. An out-of-stock SKU has
                    // no q variables (LHS identically 0), so instead of expanding phantom
                    // variables one constant constraint zfin<=0 encodes it exactly. RHS stays
                    // the REMAINING demand (r_rem): an original-demand RHS could never be met
                    // for previously-split orders (their earlier q are already committed) and
                    // would render the completion bonus unreachable (spec section 3.4).
                    foreach (var sku in residuals[order])
                    {
                        if (!PiSKU.ContainsKey(sku.Key))
                        {
                            if (sku.Value > 0)
                                wrapper.AddConstr(variablesBinary[zname] <= 0, "eZfinOOS");
                            continue;
                        }
                        var lhs = LinearExpression.Sum(deVarNameq.Where(v => v.order.ID == order.ID && v.skui.ID == sku.Key.ID).Select(v => variablesQ[v.name]));
                        wrapper.AddConstr(lhs <= sku.Value, "eM2");
                        wrapper.AddConstr(lhs >= sku.Value * variablesBinary[zname], "eZfin");
                    }
                }
                else
                {
                    foreach (var sku in residuals[order].Where(p => PiSKU.ContainsKey(p.Key)))
                    {
                        var lhs = LinearExpression.Sum(deVarNameq.Where(v => v.order.ID == order.ID && v.skui.ID == sku.Key.ID).Select(v => variablesQ[v.name]));
                        if (!crossTime)
                            wrapper.AddConstr(lhs == sku.Value * variablesBinary[zname], "eM1");
                        else
                        {
                            wrapper.AddConstr(lhs <= sku.Value, "eM2");
                            wrapper.AddConstr(lhs >= sku.Value * variablesBinary[zname], "eM2done");
                        }
                    }
                }
            }
```

（鏡像忠實性註記：`!prMode` 分支必須與原文**逐字相同**——這是 bit-identical 的載體。`prMode` 蘊含 `crossTime`，故 PR 分支不需要 eM1 支線。）

- [ ] **Step 5: (e) 決策 log 簽名與兩個呼叫點**

5-1. `WriteExactDecisionLog` 簽名（line ~170-171）改為：

```csharp
        private void WriteExactDecisionLog(bool solved, double time, int pendingOrdersN, int stationsWithCap, int podsInModel,
            int nXps, int nChildren, int nFastPath, int unitsAssigned, double sumUs, double objective, double optSec,
            int partialOrders, int partialOnlyNewTrips)
```

5-2. header 行（line ~181）改為：

```csharp
                _exactDecisionLog.WriteLine("decision,time,solved,pendingOrders,stationsWithCap,podsInModel,xps,children,fastPath,units,sumUs,objective,solveSec,partialOrders,partialOnlyNewTrips");
```

5-3. 資料行：在 `optSec.ToString(...)` 那個元素**後**加兩個元素（陣列尾端，逗號注意）：

```csharp
                optSec.ToString(System.Globalization.CultureInfo.InvariantCulture),
                partialOrders.ToString(),
                partialOnlyNewTrips.ToString()
```

5-4. 成功路徑呼叫點（line ~632-636）：在 `double sumUs = 0.0;` 迴圈之後、`WriteExactDecisionLog(true, ...)` 之前插入計算，並改呼叫：

```csharp
                // (PR diag) partial-draw observability: orders drawn from this solve without
                // earning the completion flag (deadband-break evidence, Gate 1), and
                // newly-claimed (Pa) pods whose ENTIRE draw went to such orders (the
                // fishing-for-frac guardrail, Gate 3). Computed in legacy mode too (zdonex
                // semantics there) - pure logging, zero behavior change.
                HashSet<int> zOffOrders = new HashSet<int>();
                foreach (var z in deVarNamez)
                    if (Math.Round(variablesBinary[z.name].GetValue()) == 0)
                        zOffOrders.Add(z.order.ID);
                int partialOrders = IsdeVarNameq.Where(v => zOffOrders.Contains(v.order.ID)).Select(v => v.order.ID).Distinct().Count();
                int partialOnlyNewTrips = 0;
                foreach (var x in IsdeVarNamexps)
                {
                    if (!Pa.Contains(x.pod))
                        continue;
                    var podDraws = IsdeVarNameq.Where(q => q.pod.ID == x.pod.ID && q.outputstation.ID == x.outputstation.ID).ToList();
                    if (podDraws.Count > 0 && podDraws.All(q => zOffOrders.Contains(q.order.ID)))
                        partialOnlyNewTrips++;
                }
                WriteExactDecisionLog(true, Instance.Controller.CurrentTime, pendingOrders.Count, Cs.Count, Pods.Count(),
                    IsdeVarNamexps.Count, nChildren, nFastPath, unitsAssigned, sumUs, wrapper.GetObjectiveValue(), _optSec,
                    partialOrders, partialOnlyNewTrips);
```

5-5. 失敗路徑呼叫點（line ~640-641）改為：

```csharp
                WriteExactDecisionLog(false, Instance.Controller.CurrentTime, pendingOrders.Count, Cs.Count, Pods.Count(),
                    0, 0, 0, 0, 0.0, double.NaN, _optSec, 0, 0);
```

- [ ] **Step 6: Build 驗證**

執行 Global Constraints 的 build 指令。預期：`0 個錯誤`。

- [ ] **Step 7: Diff 紀律檢查**

Run: `git diff --stat`
預期：本 task 只多出 `SplitM1GExactManager.cs` 一檔的變更。

- [ ] **Step 8: Commit（指名檔案）**

```powershell
git add RAWSimO.Core/Control/Defaults/OrderBatching/SplitM1GExactManager.cs
git commit -m "feat(pr): prMode - zfin true-completion + R/D_o pro-rata + partial-draw log columns (M2e-PR Task 2)"
```

---

### Task 3: PR sweep xconf ×3

**建議 model：haiku（拷貝微調型）**

**Files:**
- Create: `Material/Instances/CoreBenchmark/small/sweep/pr_r10.xconf`
- Create: `Material/Instances/CoreBenchmark/small/sweep/pr_r20.xconf`
- Create: `Material/Instances/CoreBenchmark/small/sweep/pr_r40.xconf`
（母本：同目錄 `sw_0_0.xconf`，即 acc_m2ea 臂的 config）

**Interfaces:**
- Produces: 三個 xconf，`<Name>` 分別為 `pr_r10`/`pr_r20`/`pr_r40`（Name 決定輸出子目錄命名，Task 5 依賴）。

- [ ] **Step 1: 拷貝三份**

```powershell
$sw = "Material/Instances/CoreBenchmark/small/sweep"
Copy-Item "$sw/sw_0_0.xconf" "$sw/pr_r10.xconf"
Copy-Item "$sw/sw_0_0.xconf" "$sw/pr_r20.xconf"
Copy-Item "$sw/sw_0_0.xconf" "$sw/pr_r40.xconf"
```

- [ ] **Step 2: 每份做兩處 Edit（保 CRLF——用 Edit 工具，不要用 Set-Content 重寫整檔）**

每個檔案改動 A（Name，R 值代入對應檔名）：

```
old: <Name>split_milp_m2ea</Name>
new: <Name>pr_r10</Name>          （pr_r20/pr_r40 依檔名類推）
```

每個檔案改動 B（OrderBatchingConfig 區塊，在 `<IdleSlotWeight>0</IdleSlotWeight>` 之後插入兩行；`<ProRataReward>` 值依檔名為 10/20/40）：

```
old:     <IdleSlotWeight>0</IdleSlotWeight>
  </OrderBatchingConfig>
new:     <IdleSlotWeight>0</IdleSlotWeight>
    <TrueCompletionReward>40</TrueCompletionReward>
    <ProRataReward>10</ProRataReward>
  </OrderBatchingConfig>
```

（XmlSerializer 元素順序：新欄位宣告於 class 尾端，序列化順位在 `PodSelectTiebreakCost` 之後；xconf 中放在 `IdleSlotWeight` 之後、且其間省略的中段欄位不出現，相對宣告順序不變，反序列化合法。）

- [ ] **Step 3: byte 級驗證（憲法規則：排除允許差異行後零差異）**

```bash
cd /c/Users/Aesop/Desktop/EE-RAWSim-O_PP/Material/Instances/CoreBenchmark/small/sweep
for f in pr_r10 pr_r20 pr_r40; do
  diff <(grep -avE "<Name>split_milp_m2ea</Name>" sw_0_0.xconf) \
       <(grep -avE "<Name>$f</Name>|<TrueCompletionReward>|<ProRataReward>" $f.xconf) \
    && echo "$f CONTENT-OK" || echo "$f MISMATCH"
  file $f.xconf; grep -c $'\r' $f.xconf
done
```

預期：三個 `CONTENT-OK`；`grep -c $'\r'` 的行數與 `grep -c $'\r' sw_0_0.xconf` 相同＋2（新增兩行也是 CRLF）。
另驗證 R 值正確：`grep -H "ProRataReward" pr_*.xconf` → 10/20/40 各就其位。

- [ ] **Step 4: Commit**

```powershell
git add Material/Instances/CoreBenchmark/small/sweep/pr_r10.xconf Material/Instances/CoreBenchmark/small/sweep/pr_r20.xconf Material/Instances/CoreBenchmark/small/sweep/pr_r40.xconf
git commit -m "feat(pr): pr_r10/20/40 sweep xconfs - B=40, R in {10,20,40} (M2e-PR Task 3)"
```

---

### Task 4: Gate 0 迴歸（controller 親自執行，不派 subagent）

**前置（controller 在派發 Task 2 之前先做）**：用**當前工作樹**（含未 commit LB 改動、含 Task 1 config 欄位——兩者對 M2e 行為皆惰性）build 一次並發射 pre-run，跑的期間並行做 Task 2/3：

- [ ] **Step 1: 寫 `run_pr_gate0_pre.cmd` 並背景發射**

```
@echo off
cd /d C:\Users\Aesop\Desktop\EE-RAWSim-O_PP
set CLI=RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe
set SM=Material\Instances\CoreBenchmark\small
"%CLI%" "%SM%\small.xlayo" "%SM%\small_o100_mu100.xsett" "%SM%\sweep\sw_0_0.xconf" "output_pr_gate0_pre" 0 > run_pr_gate0_pre.log 2>&1
echo GATE0PRE-DONE >> run_pr_gate0_pre.log
```

發射：`Start-Process -FilePath "run_pr_gate0_pre.cmd" -WindowStyle Hidden`，用 Monitor 盯 `run_pr_gate0_pre.log` 出現 `GATE0PRE-DONE`。

- [ ] **Step 2:（Task 2 commit 後）rebuild，寫 `run_pr_gate0_post.cmd`（同上，輸出改 `output_pr_gate0_post`、log 改 post）並背景發射**

- [ ] **Step 3: 遮罩比對（排除牆鐘欄位 0-based 84,85,195-223）**

```bash
cd /c/Users/Aesop/Desktop/EE-RAWSim-O_PP
mask() { awk -F';' '{for(i=1;i<=NF;i++){x=i-1; if(x==84||x==85||(x>=195&&x<=223)) $i="X"}; print}' OFS=';' "$1"; }
diff <(mask output_pr_gate0_pre/*/footprint.csv) <(mask output_pr_gate0_post/*/footprint.csv) && echo "GATE0 PASS (bit-identical)" || echo "GATE0 DIVERGED"
```

- [ ] **Step 4: 若 DIVERGED → 抖動歸因後再判**

WHCA* 有牆鐘預算（RuntimeLimitPerAgent=0.1s），同 seed 重跑本就可能微幅發散。歸因法：把 pre-run 對**歷史存檔** `output_acc_m2ea_s0` 做同樣遮罩比對——若 pre vs 歷史也發散，證明抖動存在，Gate 0 改用 KPI 容忍判準：pre/post 的 orders（欄 37，0-based）差 ≤2、items（欄 35）差 ≤3、itemPileOn（欄 121）差 ≤0.05。若 pre vs 歷史**完全一致**而 pre vs post 發散 → 真回歸，回 Task 2 找 prMode 閘漏（優先檢查 `!prMode` 分支是否逐字未動、`zRewardCoeff` 在 B=0 時是否等於 w2）。

- [ ] **Step 5: 順帶檢查決策 log 相容**：post 的 `splitm1gx_decision_log.csv` 應多出 `partialOrders,partialOnlyNewTrips` 兩欄且 B=0 之下 `partialOrders` 仍可非零（zdonex 語意的部分揀取本就存在，屬正常）；行數與 pre 相同。

- [ ] **Step 6: ledger 記錄 Gate 0 結果**

---

### Task 5: Gate 1 煙霧 + Gate 3 粗掃與驗收（controller 親自執行）

- [ ] **Step 1: 寫 `run_pr_sweep.cmd`（6 run 序列）並背景發射**

```
@echo off
cd /d C:\Users\Aesop\Desktop\EE-RAWSim-O_PP
set CLI=RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe
set SM=Material\Instances\CoreBenchmark\small
for %%r in (pr_r10 pr_r20 pr_r40) do (
  for %%s in (0 1) do (
    echo [%TIME%] %%r seed %%s >> run_pr_sweep.log
    "%CLI%" "%SM%\small.xlayo" "%SM%\small_o100_mu100.xsett" "%SM%\sweep\%%r.xconf" "output_%%r_s%%s" %%s >> run_pr_sweep.log 2>&1
  )
)
echo PRSWEEP-DONE >> run_pr_sweep.log
```

- [ ] **Step 2: Gate 1 判準（對 pr_r20 seed 0）**

```bash
cd /c/Users/Aesop/Desktop/EE-RAWSim-O_PP
awk -F';' '{printf "items=%s orders=%s placed=%s pileOn=%.3f\n", $36, $38, $28, $122}' output_pr_r20_s0/*/footprint.csv
awk -F',' 'NR>1{p+=$14} END{print "partialOrders sum =", p}' output_pr_r20_s0/*/splitm1gx_decision_log.csv
```

PASS 條件：run 正常結束（footprint 存在）；`placed − orders ≤ 15`；`partialOrders sum > 0`（pro-rata 確實觸發）。

- [ ] **Step 3: Gate 3 粗掃表 + 護欄**

```bash
for r in pr_r10 pr_r20 pr_r40; do for s in 0 1; do
  awk -F';' -v a="$r" -v s="$s" '{printf "%s;s%s;orders=%s;items=%s;pileOn=%.3f\n", a, s, $38, $36, $122}' output_${r}_s${s}/*/footprint.csv
done; done
# 護欄：只為 partial 派的新車佔比
for r in pr_r10 pr_r20 pr_r40; do for s in 0 1; do
  awk -F',' -v a="$r" -v s="$s" 'NR>1{t+=$7; p+=$15} END{printf "%s;s%s;newTrips(xps)=%d;partialOnly=%d;ratio=%.3f\n", a, s, t, p, (t>0?p/t:0)}' output_${r}_s${s}/*/splitm1gx_decision_log.csv
done; done
```

護欄觸發（任一 R 臂）：`partialOnly/xps > 0.05` 或 orders < 620 → 該 R 出局；全部出局則回頭與使用者討論（不要自行改權重）。

- [ ] **Step 4: 選 R*（seeds 0,1 平均 orders 最高者；平手取 pile-on 高者），跑 seeds 2,3,4 補全 5 seeds**（`run_pr_full.cmd`，同 Step 1 模式，單臂三 run）

- [ ] **Step 5: 驗收判定**：R* 5-seed 平均 orders vs 基準——及格 > 642.4（acc_m2ea）；達標 ≥ 645.8（PVGS 同情境水位帶 641-648 的中值以上）。連同 pile-on/item-distance（`$44/$36`＝DistanceTraveled/ItemsHandled）寫入 ledger。

---

### Task 6: Gate 2 機制探針（controller 親自執行）

- [ ] **Step 1: 用既有 `scratch_probe.xsett`（BackfillProbeEnabled=true 的 small_o100_mu100 拷貝）跑 R* seed 0/1 probe run**（`run_pr_probe.cmd`，輸出 `output_pr_probe_s0`/`_s1`；對照組數據已存在：`output_probe_m2e_s0`/`_s1`）

- [ ] **Step 2: 釋放殘量對比（Gate 2-1）**

```bash
# backfill_probe.csv 欄 9（0-based 8）= podRemainingUnits
for d in output_probe_m2e_s0 output_pr_probe_s0 output_probe_m2e_s1 output_pr_probe_s1; do
  awk -F';' 'NR>1{n++; s+=$9} END{printf "%s: releases=%d meanResidual=%.2f\n", FILENAME, n, s/n}' $d/*/backfill_probe.csv
done
```

PASS：PR 臂 meanResidual 顯著低於 M2e 臂（≈10 → 下降；方向即可，幅度記錄供論文）。

- [ ] **Step 3: pile-on 與駐留（Gate 2-2/2-3）**：pile-on 讀 footprint（目標 4.60→6+ 為強信號）；駐留用 `docs/2026-07-12-po-gap-diagnosis.md` 的視角重建方法（itemprogression.csv 以同 pod-station 連續揀貨間隔 >120s 切 visit，報 picks/visit 與 visit 時長）對 `output_pr_probe_s0` 重算一次，與該文 §視角重建 的 M2e 數值（picks/visit 2.5-2.7、dwell 35s）對照。

- [ ] **Step 4: 三探針結果 + Gate 3 驗收表寫入 ledger；向使用者總結報告（含機制故事線與 TP 預期管理——spec §5）**

---

## Self-Review 紀錄

- Spec 覆蓋：D1/D4（pro-rata、D_o）→ Task 2 Step 3；D2/D8（B 加權、B=40/R 掃描）→ Task 2 Step 1-2 + Task 3；D3（eZfin/r_rem）→ Task 2 Step 4；D5/D7（無閘門、eps=w5=0）→ 結構性（xconf 不含該欄位即預設 0）+ Task 5 護欄；D6（入場過濾不動）→ 無 task 觸碰 line 215-217 ✓;D9（旗標/bit-identical）→ Task 1 + Gate 0；D10（trigger/交付零改動）→ 無 task 觸碰 ✓。決策 log 2 欄 → Task 2 Step 5。Gate 0/1/2/3 → Task 4/5/6。
- 型別一致：`TrueCompletionReward`/`ProRataReward`（Task 1 宣告 = Task 2 讀取 = Task 3 xconf 元素名）；log 欄名 `partialOrders`/`partialOnlyNewTrips`（Task 2 header = Task 5 awk 欄位 14/15，1-based）✓。
- 佔位符掃描：無 TBD/TODO；所有程式碼逐字給出 ✓。
- 已知刻意取捨：無單元測試（Gurobi-bound，見 Global Constraints）；Gate 0 抖動 fallback 判準明文化。

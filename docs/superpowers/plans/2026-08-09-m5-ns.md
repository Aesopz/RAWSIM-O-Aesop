# 實作計畫：M5-NS（整單指派的貪婪不拆單對照）

Spec：`docs/superpowers/specs/2026-08-09-m5-ns-whole-order-greedy-design.md`
鏡像對象：`GreedyM5Manager.cs`（架構）＋ `M4GNSManager.cs`（模型／價格）
分支：`setlevel-redesign`

---

## Global Constraints

- **Build**（只能這條，且**必須用 PowerShell 工具，不可用 Bash**——Bash 會把 `/p:` `/nologo` 當路徑轉換）：
  ```
  & "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" RAWSimOWithSolverWrapping.sln /p:Platform=x64 /p:Configuration=Release /v:minimal /nologo
  ```
- **C# 7.3**（net48）。無 switch expression／record／範圍運算子。`out` 參數不可被 lambda 捕獲（CS1628）→ 用區域變數複本。
- 🚫 **絕對不可修改**：`M1GManager.cs`、`HADGSManager.cs`、`M4GManager.cs`、`M4GNSManager.cs`、`M4GPricing.cs`、`M4GNSPricing.cs`、`GreedyM5Manager.cs`、`SplitM2eICManager.cs`。
- 新檔案手動加 `<Compile Include>` 到 `RAWSimO.Core/RAWSimO.Core.csproj`。
- config 欄位**追加在類別末端**（XmlSerializer 順序敏感，不符會**靜默忽略**）。
- ⚠️ **新增 config 子類必加 `[XmlInclude(typeof(...))]`** 到 `MethodConfiguration.cs` 的 `OrderBatchingConfiguration` 屬性群（約 line 820）。**漏掉會反序列化靜默失敗**——2026-08-09 已踩過一次。
- xconf 用 PowerShell `[IO.File]::ReadAllText/WriteAllText` 產生（保 CRLF），`Compare-Object` 驗證。
- 🚫 不可 `git add -A`（`out/` 未 gitignore）。不需 commit。

---

## Task 1 — 型別註冊

**Model：haiku**

1. `MethodConfiguration.cs`：enum `OrderBatchingMethodType` **末端**追加
   ```csharp
           /// <summary>M5-NS: whole-order greedy, the no-split counterpart of HGS-M5.</summary>
           GreedyM5NS,
   ```
2. 同檔案 `OrderBatchingConfiguration` 的屬性群（`[XmlInclude(typeof(M4GNSConfiguration))]` 那行之後）追加
   ```csharp
       [XmlInclude(typeof(GreedyM5NSConfiguration))]
   ```
3. `Controller.cs`：在 `case OrderBatchingMethodType.M4GNS:` 之後、`default:` 之前
   ```csharp
                   case OrderBatchingMethodType.GreedyM5NS: OrderManager = new GreedyM5NSManager(instance); break;
   ```
4. `MethodConfigurationsOB.cs` **檔案末端**（`M4GNSConfiguration` 之後）追加：
   ```csharp
       /// <summary>
       /// M5-NS: the greedy counterpart of M4G-NS. Same decision structure (M1G's whole-order
       /// assignment), same price list (mu, delta, sigma = SlotScale * mu), same solution space -
       /// the only permitted difference is that a greedy construction replaces the MILP solve.
       /// Reuses M4GNSPricing rather than declaring its own, which is what structurally guarantees
       /// the two arms price identically.
       /// </summary>
       public class GreedyM5NSConfiguration : M1GConfiguration
       {
           /// <summary>Returns the type of the method.</summary>
           public override OrderBatchingMethodType GetMethodType() { return OrderBatchingMethodType.GreedyM5NS; }

           /// <summary>Scales mu after it is measured. 1.0 = pure measurement.</summary>
           public double MuScale = 1.0;
           /// <summary>Scales delta after it is measured. 1.0 = pure measurement.</summary>
           public double DeltaScale = 1.0;
           /// <summary>Completed orders required before mu is measured rather than taken from MuFallback.</summary>
           public int WarmupOrders = 50;
           /// <summary>Warm-up metres per completed order.</summary>
           public double MuFallback = 24.0;
           /// <summary>Warm-up realisation rate.</summary>
           public double DeltaFallback = 0.05;
           /// <summary>Pins mu when &gt; 0 (ablation only).</summary>
           public double MuFixed = 0;
           /// <summary>Pins delta when &gt; 0 (ablation only).</summary>
           public double DeltaFixed = 0;
           /// <summary>Stratify delta by committed-pod count, same key M4G-NS uses.</summary>
           public bool StratifiedDelta = true;
           /// <summary>Minimum valued orders in a stratum before its own ratio is trusted.</summary>
           public int DeltaStratumMinOrders = 50;
           /// <summary>Idle-slot price as a multiple of mu; mirrors M4GNSConfiguration.SlotScale.</summary>
           public double SlotScale = 1.0;
           /// <summary>Outer mu iterations per decision (greedy Dinkelbach).</summary>
           public int MuIterations = 5;
           /// <summary>Stop the mu loop once |objective| falls below this.</summary>
           public double MuTolerance = 0.5;
           /// <summary>Max doublings of mu when the plan degenerates to "do nothing".</summary>
           public int MuEscalations = 6;
           /// <summary>Score an assign move by its TRUE marginal change: an order already counted by
           /// the valuation layer only improves by mu*(1-delta), not mu. Mirrors M5's FaithfulMarginal.</summary>
           public bool FaithfulMarginal = true;
       }
   ```
5. `RAWSimO.Core.csproj`：在 `M4GNSPricing.cs` 那行之後追加
   ```xml
       <Compile Include="Control\Defaults\OrderBatching\GreedyM5NSManager.cs" />
   ```

⚠️ `GreedyM5NSConfiguration` **不是** `IM4GPrices`，但要能餵給 `M4GNSPricing`。
`M4GNSPricing` 的建構子目前吃 `M4GNSConfiguration`。**Task 2 必須處理這個型別問題**（見下）。

---

## Task 2 — `GreedyM5NSManager.cs`

**Model：sonnet**

### 2.0 價格物件的型別問題（先解決）

`M4GNSPricing(M4GNSConfiguration)` 只接受 M4G-NS 的 config。三個選項，**採第 3 個**：

1. ~~改 `M4GNSPricing`~~ → 禁改
2. ~~讓 `GreedyM5NSConfiguration` 繼承 `M4GNSConfiguration`~~ → 會讓 `is M4GNSConfiguration` 檢查誤判
3. ✅ **在 `GreedyM5NSManager` 內部建一個 `M4GNSConfiguration` 實例**，把自己 config 的
   `MuScale/DeltaScale/WarmupOrders/MuFallback/DeltaFallback/MuFixed/DeltaFixed/StratifiedDelta/DeltaStratumMinOrders`
   逐欄複製過去，再 `new M4GNSPricing(那個實例)`。
   **在建構子內完成，並加註解說明為何這樣做。** 這保證兩臂價格公式逐字相同。

### 2.1 架構鏡像 `GreedyM5Manager.cs`

| 來源（GreedyM5Manager.cs） | 目標 | 修改 |
|---|---|---|
| `class M5EpochState`（~123） | `M5NSEpochState` | 刪 `PaBound/PaDrawn/DrawCache/DrawTierCache/OrdersBySku`（皆為 ρ／V2a 專用）；`Residuals` → `Demands`（**原始需求**，非殘餘）；新增 `AssignedOrders` |
| `CloneForTrial`（~227） | 同 | 沿用寫時複製；複製欄位隨上表調整 |
| `BuildEpochState`（~363） | 同 | 由 `order.Positions` 建 `Demands`；**只納入全部線都有覆蓋的訂單** |
| `ValuationSweep`（~597） | `ValuationSweepNS` | 見 2.3 |
| `class M5LineMove`（~630） | `M5NSAssignMove` | 欄位：`Order`、`StationIndex`、`Delta`、`AlreadyValued` |
| `EnumerateLineMoves`（~688） | `EnumerateAssignMoves` | 見 2.2 |
| `ApplyToBooks`（~745） | 同 | 扣掉該站台庫存中該訂單的全部需求；`FreeSlots[s]--`；加入 `AssignedOrders` |
| `ApplyDispatchToBooks`（~780） | 同 | 逐字（去掉 DrawCache 失效那段） |
| `EvaluateDispatch`（~885） | 同 | 逐字，`EnumerateLineMoves` → `EnumerateAssignMoves` |
| `BuildPlan`（~950） | 同 | 逐字 |
| `CommitParts`（~1010） | `CommitAssignments` | **大幅簡化**：只 `AllocateOrder(order, station)`。🚫 不建子單、不寫 splitorders.csv、不用 `SplitConsolidationLogger` |
| `DecideAboutPendingOrders`（~1150） | 同 | μ 迭代取代 λ 迭代（見 2.4） |
| `WriteDecisionLog`（~1140） | 同 | 表頭見 2.5 |

> **鏡像失真才是缺陷，鏡像本身是 spec 要求。** Reviewer 須逐項比對來源與目標的語意等價性。

### 2.2 `EnumerateAssignMoves`（**新程式碼**）

```
foreach order in st.ScanOrder:
    if st.AssignedOrders.Contains(order): continue          # shi2
    foreach s in 0..StationList.Count-1:
        if st.FreeSlots[s] <= 0: continue                   # shi4，硬約束
        # shi5：該站台必須持有這張單的「全部」需求
        if 任一 sku 的 st.PerStationAvail[s][sku] < Demands[order][sku]: continue
        move.Delta = FaithfulMarginal && move.AlreadyValued
                     ? -mu * (1.0 - delta) - sigma
                     : -mu - sigma
        moves.Add(move)
```

`AlreadyValued` = 該訂單是否在**本次列舉前**的 `ValuationSweepNS` 基準集合中。
基準集合每輪主迴圈算一次即可（不必每個候選移動重算）。

⚠️ **槽位與空閒機器人是硬約束，永不進入計分。** 這對應 M4G-NS 的 shi4，不是把它折算成懲罰項。

### 2.3 `ValuationSweepNS`（**新程式碼**）

```
stock ← 綁定後剩餘的 PerStationAvail 副本
valuedOnly = 0
foreach order in st.ScanOrder:
    if st.AssignedOrders.Contains(order): continue
    foreach s in 各站台:                                     # 無槽位限制
        if stock[s] 能覆蓋 order 全部需求:
            扣除；valuedOnly++；break                        # shi2：一單一站
```

### 2.4 目標值與 μ 迭代

```
plan.Objective = Σ(接受的移動 Δ)  +  Σ(派車距離)  −  mu * delta * valuedOnlyOrders
plan.DStar     = Σ(派車距離)
sigma          = SlotScale * mu        ← 每次 mu 變動都要重算
```

μ 迭代逐字鏡像 `GreedyM5Manager.cs:1178-1228`（含雙向升壓），λ→μ，並在每次 `BuildPlan` 前更新 `sigma`。

### 2.5 決策日誌

`m5ns_decision_log.csv`，表頭：
```
decision,time,pendingOrders,stations,pods,mu,delta,sigma,assignedOrders,valuedOnlyOrders,
dispatches,objective,dStar,muIters,muStart,muEnd,decisionSec
```
🚫 **不得有 lambda／rho／epsilon 欄位。**

### 2.6 價格結算（放在 WriteDecisionLog 之後）

```csharp
_pricing.RegisterCompletedOrders(st.AssignedThisEpoch);
_pricing.RegisterDecision(st.AssignedThisEpoch,
    st.AssignedThisEpoch + plan.ValuedOnlyOrders, deltaStratum);
```
`deltaStratum = StratifiedDelta ? Math.Min(Pb.Count, 3) : -1`（**與 M4G-NS 同鍵**）。

---

## Task 3 — xconf

**Model：haiku**

由 `Material/Instances/CoreBenchmark/small/m4g_ns.xconf` 產生 `hgs_m5_ns.xconf`：
`<OrderBatchingConfig xsi:type="M4GNSConfiguration">` → `xsi:type="GreedyM5NSConfiguration"`，
元素依 `GreedyM5NSConfiguration` 的**欄位宣告順序**排列：

```xml
  <OrderBatchingConfig xsi:type="GreedyM5NSConfiguration">
    <Name />
    <TieBreaker>EarliestDueTime</TieBreaker>
    <FastLane>true</FastLane>
    <LateBeforeMatch>false</LateBeforeMatch>
    <FastLaneTieBreaker>EarliestDueTime</FastLaneTieBreaker>
    <MuScale>1</MuScale>
    <DeltaScale>1</DeltaScale>
    <WarmupOrders>50</WarmupOrders>
    <MuFallback>24</MuFallback>
    <DeltaFallback>0.05</DeltaFallback>
    <MuFixed>0</MuFixed>
    <DeltaFixed>0</DeltaFixed>
    <StratifiedDelta>true</StratifiedDelta>
    <DeltaStratumMinOrders>50</DeltaStratumMinOrders>
    <SlotScale>1</SlotScale>
    <MuIterations>5</MuIterations>
    <MuTolerance>0.5</MuTolerance>
    <MuEscalations>6</MuEscalations>
    <FaithfulMarginal>true</FaithfulMarginal>
  </OrderBatchingConfig>
```

同時產生退化檢查用的 `hgs_m5_ns_s9.xconf`：`MuFixed=40`、`DeltaFixed=1`、`SlotScale=25`、`MuIterations=0`。

---

## Task 4 — 驗收

**Model：sonnet**

Build 後跑 seed 0：
```
RAWSimO.CLI\bin\x64\Release\RAWSimO.CLI.exe `
  Material\Instances\CoreBenchmark\small\small_6bot.xlayo `
  Material\Instances\CoreBenchmark\small\small_o100_mu100_2h_inv70.xsett `
  Material\Instances\CoreBenchmark\small\hgs_m5_ns.xconf out\m5ns_dev 0
```

| | 檢查 | 通過條件 |
|---|---|---|
| S-1/S-2 | 無拆單、無子單 | 不產生 `splitorders.csv`；每張單只被指派一次 |
| S-3 | 價目表 | 日誌表頭無 lambda/rho/epsilon |
| S-4 | μ 校準 | μ ≈ 累積距離 / 累積完成單 |
| S-5 | δ 校準 | ∈[0,1]，且**與 M4G-NS 的 δ 同量級**（對照 `out/nsv2_s0`）|
| S-6 | 不崩潰 | 全場無例外 |
| S-7 | 正典不變 | 重跑 `m4g_ns.xconf` seed 0，與 `out/nsv2_s0` 逐位相同（排除 `StatTiming*`/`StatRealTimeUsed`/`StatMaxMemoryUsed`）|
| **S-8** | **最佳性間隙** | 與 `out/nsv2_s0` 同 seed 比對，報告 EOR/訂單差；M5-NS 應**略遜**（量級參考 M5 vs M4G）|
| **S-9** | **退化檢查** | 跑 `hgs_m5_ns_s9.xconf`，行為應接近 M1G（對照 `out/ns3_m1g_s0`）|

**S-9 是最重要的正確性檢查**——把三個價格釘回 M1G 手調值就該回到 M1G。做不到代表結構沒對齊。
（2026-08-09 的 M4G-NS v1 就是缺這個檢查才錯了一輪。）

---

## 最終審查

全 diff range 一次審查，二元結論 **READY TO MERGE: Yes / No**。重點：
1. 移動集合**未逸出** M4G-NS 的解空間（S-8 的前提）
2. σ 隨 μ 同步縮放（否則 `V*=(D*−obj)/μ` 靜默算錯）
3. 硬約束（槽位、機器人）**未被轉成價格**
4. `M4GNSPricing` 重用正確、δ 定義與 M4G-NS 逐字相同
5. 禁改檔案未被觸碰
